#!/bin/sh

KDIR=${KDIR:-/lib/modules/`uname -r`/build}

make -C ${KDIR} M=$(pwd) modules > /dev/null 2>&1

if [ $? -eq 0 ] ; then
	echo "#define HAVE_IOV_ITER_GET_PAGES 1"
else
	echo "#define HAVE_IOV_ITER_GET_PAGES 0"
fi
